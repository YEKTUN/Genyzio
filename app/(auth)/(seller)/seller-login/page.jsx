import LoginForm from "@/app/(auth)/(client)/_components/authComponents/LoginForm";
import React from "react";

const page = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default page;
